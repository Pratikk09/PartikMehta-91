let express,application;_8ce‍.w("express",[["default",["express"],function(v){express=v}],["application",["application"],function(v){application=v}]]);
const app = express();
const { APP_PORT } = require("./config");
const router = require('./routes');

app.use('/api',router)


app.listen(APP_PORT, () => {
  console.log(`Server is running on port 4000 ${APP_PORT}`);
});
